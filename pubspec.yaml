name: character_gpt
description: "성격을 가진 GPT 채팅"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http:
  vibration:
  rxdart:
  intl:
  provider:
  flutter_dotenv:
  rive:

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/.env
    - assets/rive/

